{
  "name": "lexie-new",
  "private": true,
  "description": "Standalone OpenClaw Railway service for Lexie cutover",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "lint": "node --check server.js && node --check session-store.js && node --check ops-gateway.js && node --check auth.js",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "ws": "^8.18.3"
  }
}
